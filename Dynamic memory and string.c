#include <stdio.h>
#include <stdlib.h>

int main()
{
    char *s[4];        /* input string */
   // s=malloc(10* sizeof(s));
   printf("\n Enter the string\t");
    //gets(s);
   scanf("%s",*s[0]);
    printf("\n\n%s",*s[0]);
}
